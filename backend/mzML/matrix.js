
module.exports.createMatrix = (dataMZML, callback) => {
    matrix = [];



    //Percorrer os scans
        //Percorrer as massas e intensidade ao mesmo tempo    
            //Guardar no matrix[scanid][mass] = intensidade
    
        
    //Quando a matrix esta pronta:
    callback(matrix)    
}
